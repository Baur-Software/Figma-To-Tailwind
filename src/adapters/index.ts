/**
 * Adapters Module
 *
 * Input adapters parse external data sources into the normalized ThemeFile format.
 * Output adapters transform the normalized ThemeFile format into target outputs.
 */

// Input Adapters
export * from './input/index.js';

// Output Adapters
export * from './output/index.js';
